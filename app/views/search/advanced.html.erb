<% form_tag url_for(:action => 'advanced_result') do %>
  <p>Enter how much data you want to use to find the best deal</p>
  <%= label_tag "Usage: " %>
  <%= text_field_tag "usage" %>
  <%= select_tag "usage_unit", options_for_select(%w(KB MB GB)) %>
  <br />
  <%= label_tag "Country: " %>
  <%= select_tag("country_id", options_for_select(@countries.map {|c| [c.country, c.id]})) %>
  <br />
  <br />
  <%= submit_tag('Search') %>
<% end %>